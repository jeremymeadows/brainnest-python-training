# Write a program which prompts the user for a Celcius temperature, converts
# the temperature to Fahrenheight, and print out the converted temperature.

c = float(input('Enter temperature (Celcius): '))
f = c * 1.8 + 32
print(f'Temperature in Fahrenheight is {f}')
